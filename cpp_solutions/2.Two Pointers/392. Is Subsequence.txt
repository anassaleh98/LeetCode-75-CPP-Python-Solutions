class Solution {
public:
    bool isSubsequence(string s, string t) {
        int p1=0, p2=0, c=0; 
        while(p1<s.size() and p2 < t.size()){
            if(s[p1]==t[p2])  p1++, p2++, c++;
            else p2++;
        }
        if(c== s.size()) return true;
        else return false;
    }
};