class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int n = gain.size()+1, mx=0;
        int arr[n];
        arr[0]=0;
        for(int i=1; i<n; i++){
            arr[i] = arr[i-1] + gain[i-1];
            if(arr[i]>mx) mx = arr[i];
        }
        return mx;
    }
};